## Summary

What changed?

## Authorization

- **Authorizing issue:** `none`
- **Emitted decision:** `none`
- **Hard boundaries touched:**
  - [ ] `docs/contracts/**`
  - [ ] `src/contracts/**`
  - [ ] `exports/promoted/**`
  - [ ] `schemas/**`
  - [ ] `**/*.schema.json`
  - [ ] `data/fixtures/team_week_raw_governed/**`
  - [ ] none of the above
- **Promoted artifacts changed:** no
- **Merge authorization:** `operator_approval_required`

Complete every field above. Link the authorizing issue when one exists; otherwise
leave `none`. Use the exact emitted decision string, or `none`. Select every
hard boundary touched, or select only `none of the above`; the checklist paths
mirror the `MERGE_POLICY` table in `CLAUDE.md`. `Merge authorization`
must be `operator_approval_required` in v0. `agent_mergeable_after_checks` is
reserved and invalid until `CLAUDE.md` declares at least one eligible path.

## Motivation

Why was this change needed?

## Implementation

How was it implemented?

## Validation

How was it tested or verified?

## Related issue

Closes #

## Follow-up

Any remaining work, known limitations, or future improvements.
